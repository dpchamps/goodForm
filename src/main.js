/* goodForm main */

// Base function.
var goodForm = function() {
  // Add functionality here.
  return true;
};


// Version.
goodForm.VERSION = '0.0.0';


// Export to the root, which is probably `window`.
root.goodForm = goodForm;
